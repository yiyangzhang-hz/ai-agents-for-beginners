<!--
CO_OP_TRANSLATOR_METADATA:
{
  "original_hash": "c4be907703b836d1a1c360db20da4de9",
  "translation_date": "2025-07-12T14:14:15+00:00",
  "source_file": "11-mcp/code_samples/github-mcp/MCP_SETUP.md",
  "language_code": "ru"
}
-->
# Руководство по интеграции MCP Server

## Требования
- Установлен Node.js (версия 14 или выше)
- Менеджер пакетов npm
- Python с необходимыми зависимостями

## Шаги настройки

1. **Установите пакет MCP Server**
   ```bash
   npm install -g @modelcontextprotocol/server-github
   ```

2. **Запустите MCP Server**
   ```bash
   npx @modelcontextprotocol/server-github
   ```
   Сервер должен запуститься и показать URL для подключения.

3. **Проверьте подключение**
   - Обратите внимание на иконку вилки (🔌) в интерфейсе Chainlit
   - Рядом с иконкой должна появиться цифра (1), что означает успешное подключение
   - В консоли должно появиться сообщение: "GitHub plugin setup completed successfully" (а также дополнительные строки статуса)

## Устранение неполадок

### Частые проблемы

1. **Конфликт портов**
   ```bash
   Error: listen EADDRINUSE: address already in use
   ```
   Решение: измените порт с помощью:
   ```bash
   npx @modelcontextprotocol/server-github --port 3001
   ```

2. **Проблемы с аутентификацией**
   - Убедитесь, что учетные данные GitHub настроены корректно
   - Проверьте, что в файле .env содержатся необходимые токены
   - Убедитесь в доступе к GitHub API

3. **Не удалось подключиться**
   - Проверьте, что сервер запущен на ожидаемом порту
   - Проверьте настройки брандмауэра
   - Убедитесь, что в Python окружении установлены все необходимые пакеты

## Проверка подключения

Ваш MCP сервер считается правильно подключенным, если:
1. В консоли отображается "GitHub plugin setup completed successfully"
2. В логах подключения видно "✓ MCP Connection Status: Active"
3. Команды GitHub работают в чате

## Переменные окружения

Обязательны в вашем файле .env:
```
GITHUB_TOKEN=your_github_token
MCP_SERVER_PORT=3000  # Optional, default is 3000
```

## Тестирование подключения

Отправьте в чат это тестовое сообщение:
```
Show me the repositories for username: [GitHub Username]
```
При успешном ответе будет показана информация о репозитории.

**Отказ от ответственности**:  
Этот документ был переведен с помощью сервиса автоматического перевода [Co-op Translator](https://github.com/Azure/co-op-translator). Несмотря на наши усилия по обеспечению точности, просим учитывать, что автоматический перевод может содержать ошибки или неточности. Оригинальный документ на его исходном языке следует считать авторитетным источником. Для получения критически важной информации рекомендуется обращаться к профессиональному переводу, выполненному человеком. Мы не несем ответственности за любые недоразумения или неправильные толкования, возникшие в результате использования данного перевода.